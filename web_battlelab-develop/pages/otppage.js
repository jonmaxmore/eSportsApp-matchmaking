import Layout from '../components/layout'
import OTP from '../components/otp'

const OTPpage = (props) => {
    return (
        <Layout>
            <OTP />
        </Layout>
    )
}

export default OTPpage